import * as React from 'react';
import Root from 'containers/Root';
import './App.scss';

class App extends React.Component {
  public render() {
    return (
      <Root />
    );
  }
}

export default App;
